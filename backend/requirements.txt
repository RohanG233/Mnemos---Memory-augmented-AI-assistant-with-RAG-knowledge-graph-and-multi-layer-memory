# ============================================================
# Mnemos Backend — Production Dependencies
# Target: Python 3.11, Linux (Render)
#
# Versions sourced from:
#   - Original project requirements.txt (packages unique to this env)
#   - System pip show (packages installed in active Python)
# ============================================================

# -----------------------------------------------
# Web Framework
# -----------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.51.0
starlette==1.6.0
python-multipart==0.0.32

# -----------------------------------------------
# Configuration
# -----------------------------------------------
python-dotenv==1.2.2
pydantic==2.13.4
pydantic_core==2.46.4
pydantic-settings==2.14.2
annotated-types==0.8.0
typing_extensions==4.16.0

# -----------------------------------------------
# Authentication
# -----------------------------------------------
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
requests-oauthlib==2.0.0
oauthlib==3.3.1

# -----------------------------------------------
# MongoDB
# -----------------------------------------------
pymongo==4.17.0
dnspython==2.8.0

# -----------------------------------------------
# ChromaDB + Embeddings
# -----------------------------------------------
chromadb==1.5.9
sentence-transformers==5.6.0
transformers==5.12.1
tokenizers==0.22.2
safetensors==0.8.0
huggingface_hub==1.20.1

# -----------------------------------------------
# PyTorch — CPU only
# -----------------------------------------------
torch==2.12.1

# -----------------------------------------------
# NLP / Retrieval
# -----------------------------------------------
bm25s==0.3.10
PyStemmer==3.1.0
langchain-text-splitters==1.1.2
langchain-core==1.5.3

# -----------------------------------------------
# Knowledge Graph
# -----------------------------------------------
networkx==3.6.1

# -----------------------------------------------
# HTTP
# -----------------------------------------------
requests==2.33.1
httpx==0.28.1

# -----------------------------------------------
# Data / Compute
# -----------------------------------------------
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0

# -----------------------------------------------
# Utilities
# -----------------------------------------------
# bson is bundled with pymongo — do NOT install standalone bson
anyio==4.14.0
packaging==26.2
tqdm==4.69.0

# -----------------------------------------------
# OpenTelemetry (required by chromadb)
# -----------------------------------------------
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0

# -----------------------------------------------
# gRPC (required by chromadb opentelemetry exporter)
# -----------------------------------------------
grpcio==1.83.0
